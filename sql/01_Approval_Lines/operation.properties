type=etl
table=approval_lines
# source / target datasource names are optional (default from application.yml defaults.*)
# source=legacy-erp
# target=new-core
description=Approval line master (MSSQL dbo.approval_lines -> MariaDB approval_lines)
